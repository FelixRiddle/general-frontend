
/**
 * Menu that shows multiple status messages
 * 
 * It should be able to:
 * * Clear all messages
 * * Clear single message
 * 
 * [Data table]
 * I think this is similar to rows in a table of a single column.
 */
export default function StatusMessages() {
    
}
