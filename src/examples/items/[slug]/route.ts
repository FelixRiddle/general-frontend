// Now it says that Request doesn't exists

// /**
//  * 
//  */
// export async function GET({
//     request: Request,
//     { params }: { params: { slug: string } }
// }) {
//     const slug = params.slug;
    
// }
